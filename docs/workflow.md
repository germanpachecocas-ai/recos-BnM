## Sheet1
| Integrante | Nivel | Épica | Historias de Usuario (PRD §7) | Módulo asignado | Entregable — miércoles 10 jun | Dependencias — Inputs / Outputs | Complejidad |
| --- | --- | --- | --- | --- | --- | --- | --- |
| González Habib, Andrés | Medio | Épica 1 | HU1.1 — Registro simplificado (Email + Google)\nHU1.2 — Calibración por swipe: persistir prefs en users.prefs | Firebase Auth + middleware de autenticación en API | Login/registro con Email y Google. Rutas protegidas en React (redirect si no autenticado). Middleware auth.js en api/middleware/ que verifica Firebase ID Token para todos los endpoints. Documento users/{userId} creado al primer registro. | OUTPUT → Firebase ID Token disponible para todo el equipo. \nTodos los módulos dependen de este. Israel debe tener schema users listo. | Alta |
| Serranía Reinada, Manuel | Medio | Épica 3 / Infra | HU3.1 — Ingesta de catálogo: job Cloud Run que sincroniza TMDB y Google Books → colección content\nHU3.1 — Score normalizado: score = 0.7\*norm(popularity) + 0.3\*norm(rating) | Cloud Run ingest job + lógica de scoring (services/scoring.js) | Script tmdb\_ingest.py + books\_ingest.py en Cloud Run. ≥500 items en content con type, title, cover, year, genres, synopsis, popularity, rating, watchProviders, syncedAt. Cloud Scheduler cron 0 4 \* \* \*. Módulo scoring.js para GET /api/feed. | INPUT → Schema content de Israel (SCHEMA.md).\nOUTPUT → Colección content poblada. Luis, Héctor y Juan Carlos dependen de este. | Alta |
| Pérez García, Israel | Medio | Épica 1–5 / Infra | HU1.1 — Colección users (campos: email, displayName, authProvider, createdAt, prefs)\nHU3.1 — Colección content (catálogo cacheado)\nHU3.2 — Colección swipes\nHU5.1 — Colección collections | Schema Firestore + reglas de seguridad + índices | 4 colecciones creadas con campos exactos del PRD §6. Reglas de seguridad del PRD §9 desplegadas. firestore.indexes.json con índice compuesto (type + genres). SCHEMA.md documentado en repo. Emulator configurado para desarrollo local. | OUTPUT → Schema y reglas disponibles para todo el equipo.\nCamino crítico: Andrés, Manuel, Luis, Héctor dependen de este. | Alta |
| Téllez Domínguez, Luis | Bajo | Épica 3 | HU3.1 — Endpoint GET /api/feed\nHU3.2 — Endpoint POST /api/swipe | API Cloud Run — feed y registro de swipes | GET /api/feed: recibe userId, type, cursor; devuelve [{contentId, title, cover, genres, rating, synopsis}] usando scoring.js de Manuel, excluyendo IDs ya en swipes del usuario. POST /api/swipe: guarda {userId, contentId, contentType, action, timestamp} en colección swipes. HTTP 204. | INPUT → middleware auth.js (Andrés), scoring.js (Manuel), schema swipes (Israel).\nOUTPUT → Endpoints consumidos por Monserrat (SwipeDeck) y Edgar C. (ContentCard). | Media |
| Morales Marbán, Héctor | Bajo | Épica 4 | HU4.1 — Endpoint GET /api/content/{id} con watchProviders | API Cloud Run — detalle de contenido | GET /api/content/{id}: devuelve objeto content completo desde Firestore + watchProviders (TMDB Watch Providers API; placeholder neutro si no hay dato, NO inventar disponibilidad). Atribución TMDB requerida por PRD §4. | INPUT → middleware auth.js (Andrés), schema content (Israel), colección content poblada (Manuel).\nOUTPUT → Endpoint consumido por Marina (DetailSheet). | Media |
| Ruiz Hurtado, Christian | Bajo | Épica 5 | HU5.1 — Endpoints CRUD /api/collections (POST, PATCH, DELETE) | API Cloud Run — escritura de colecciones | POST /api/collections: {userId, contentId, listName, personalNote} → HTTP 201 + doc en Firestore. PATCH /api/collections/{id}: actualiza personalNote y listName → HTTP 200. DELETE /api/collections/{id} → HTTP 204. Mismo patrón de auth que Luis y Héctor. | INPUT → middleware auth.js (Andrés), schema collections (Israel), patrón de API (Luis).\nOUTPUT → Endpoints consumidos por Diana (biblioteca) y Marina (botón Guardar). | Media |
| Coronel Navarrete, Edgar | Bajo | Épica 3 | HU3.2 — Componente ContentCard: portada, título, género, descripción breve y calificación | UI — componente ContentCard | Componente React <ContentCard /> con cover, title, genres, rating, synopsis breve. Animación de entrada fluida. Props bien tipadas. Pantalla de prueba con datos mock mientras Luis termina la API. | INPUT → rutas protegidas de Andrés.\nOUTPUT → Componente consumido por Monserrat (SwipeDeck) y Marina (DetailSheet usa los mismos datos). | Media |
| Miranda Olivas, Monserrat | Bajo | Épica 3 | HU3.2 — SwipeDeck: gestos táctiles, pre-fetching al quedar 5 tarjetas, POST /api/swipe no bloqueante | UI — SwipeDeck (mecánica de swipe) | Componente <SwipeDeck /> con gestos touch/click derecha (like) / izquierda (dislike) a ≈60 FPS usando react-tinder-card o Framer Motion. Pre-fetch silencioso al quedar 5 tarjetas → GET /api/feed. Cada swipe → POST /api/swipe no bloqueante. | INPUT → ContentCard (Edgar C.), GET /api/feed y POST /api/swipe (Luis), auth token (Andrés).\nOUTPUT → Swipes registrados en Firestore. Diana (biblioteca refleja likes). | Media |
| García del Buey, Marina | Bajo | Épica 4 | HU4.1 — Vista expandida: sinopsis, autor/director, año, género, watchProviders\nHU4.1 — Tres acciones flotantes: Guardar, No me interesa, Compartir | UI — DetailSheet (pantalla de detalle) | Componente modal/slide-up que NO destruye el stack de swipe inferior. Consume GET /api/content/{id} (Héctor). Muestra watchProviders o placeholder neutro. Botón Guardar → POST /api/collections (Christian). Botón No me interesa → POST /api/swipe dislike (Luis). Botón Compartir (básico). | INPUT → GET /api/content/{id} (Héctor), POST /api/collections (Christian), ContentCard dispara la apertura (Edgar C.).\nOUTPUT → Contenido guardado en biblioteca de Diana. | Media |
| Macías Mayen, Juan Carlos | Bajo | Épica 1–2 | HU1.2 — Componente de onboarding: barra de progreso, 5-10 tarjetas, selección de géneros\nHU2.1 — Tab Selector Movies/Books con estado global active\_feed | UI — Onboarding + Tab Selector | Componente de onboarding: barra de progreso, máx 10 tarjetas saltables, selección de géneros/autores, marca prefs.cold\_start\_done = true al terminar. Tab Selector Movies/Books: indicador visual de alta visibilidad del modo activo, estado global active\_feed independiente por feed. | INPUT → Andrés (auth para guardar prefs), Manuel (catálogo para tarjetas de onboarding).\nOUTPUT → users.prefs.genres disponible para Luis (feed filtrado). Tab Selector usado por Monserrat (SwipeDeck). | Media |
| Álvarez Varela, Diana | Bajo | Épica 5 | HU5.1 — Biblioteca: colecciones Películas/Libros separadas, listas personalizadas, nota personal por ítem\nHU5.1 — GET /api/collections con filtros type y listName | UI — Biblioteca y colecciones | Vista de biblioteca accesible en 1 paso desde barra inferior. Filtros por tipo y listName. Cada ítem: cover, title, savedAt, personalNote editable (PATCH /api/collections/{id} de Christian). Crear nueva lista personalizada (POST /api/collections con nuevo listName). | INPUT → Andrés (auth), GET /api/collections (Héctor), PATCH/POST collections (Christian), swipes de Monserrat generan los ítems guardados.\nOUTPUT → Vista final de valor para el usuario. | Media |
| Pacheco Castillo, Germán | Bajo | Infra / §12 | PRD §12 — CI/CD: GitHub Actions → Firebase deploy\nPRD §9 — PWA: Service Worker offline (shell + últimos 10 ítems) | CI/CD + Firebase Hosting + Service Worker PWA | GitHub Action: push a main → npm run build → firebase deploy --only hosting. Service Worker (sw.js): cachea shell PWA + últimos 10 ítems de colección para offline parcial. Variables de entorno en README. "Hello world" desplegado antes del sábado 7 para validar el pipeline. | INPUT → Estructura base del frontend (Andrés).\nOUTPUT → URL pública de Firebase Hosting disponible para demo del Dr. el martes 10. | Media |
| Chaparro Ximello, Ulises | Bajo | QA / §10 | PRD §10 — Eventos de analítica: onboarding\_completed, swipe, feed\_exhausted, detail\_opened, content\_saved\nPRD §8 — Verificar los 7 contratos de API | Testing QA + documentación de APIs + ROADMAP.md | Suite de pruebas manuales: happy path completo (registro → onboarding → swipe → detalle → guardar). Casos borde: feed agotado, contenido ya swipeado, sin watchProviders. Colección Postman/Bruno con los 7 endpoints del PRD §8. Verificar eventos de analítica §10. Redactar docs/ROADMAP.md con las 3 fases del PRD §13. | INPUT → Luis y Héctor (APIs listas), app desplegada (Germán).\nOUTPUT → docs/ROADMAP.md listo para el Dr. Suite de pruebas entregada el 16. | Baja |